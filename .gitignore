.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.vscode/settings.json

# Real captured photos of the physical camera location, not project source.
backend/snapshots/
backend/training_data/

# Per-run server session logs (telemetry capture, see app/session_log.py).
backend/logs/

# Claude Code session-local artifacts (contain machine-specific paths and,
# in settings.local.json's case, the real camera IP in an allowlist entry).
.claude/settings.local.json
.claude/scheduled_tasks.lock
